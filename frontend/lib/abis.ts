/**
 * Minimal WoolFi ABIs as `as const` arrays so wagmi's type inference works end-to-end.
 * Only the functions the frontend uses - extend per feature as the dashboard grows.
 */
export * from "./abis/index";
