# Foundry build & sim artifacts
out/
cache/
broadcast/

# Env / secrets — NEVER commit
.env
.env.*
!.env.example
*.key
keystore/

# Node / frontend / indexer
node_modules/
.next/
.next-dev/
.ponder/
dist/
*.tsbuildinfo
# Generated by `ponder codegen` / `ponder dev` on first run
indexer/ponder-env.d.ts
indexer/generated/
indexer/src/api/
indexer/package-lock.json
# Root-level package.json from local tooling (not the repo's source of truth)
/package.json
/package-lock.json

# Python
__pycache__/
*.pyc

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Tooling / sessions
.claude/
.cursor/
.launch-state/
CLAUDE.md
slither.config.json
*.log

# Stray cloud-sandbox artifacts (canonical OZ lives in lib/openzeppelin-contracts)
/openzeppelin-contracts/

# Editor-created duplicates (e.g. Finder "Foo 2.toml" copies)
* 2.*
* copy.*
* copy *.*
